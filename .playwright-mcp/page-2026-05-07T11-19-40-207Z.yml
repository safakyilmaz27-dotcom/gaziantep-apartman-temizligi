- generic [ref=e2]: "// Mobil menÃ¼ const menuToggle = document.getElementById('menuToggle'); const nav = document.getElementById('nav'); const header = document.getElementById('header'); menuToggle.addEventListener('click', () => { nav.classList.toggle('open'); }); // MenÃ¼ linkine tÄ±klayÄ±nca kapansÄ±n document.querySelectorAll('.nav a').forEach(link => { link.addEventListener('click', () => nav.classList.remove('open')); }); // Scroll'da header gÃ¶lgesi window.addEventListener('scroll', () => { if (window.scrollY > 10) header.classList.add('scrolled'); else header.classList.remove('scrolled'); }); // Form gÃ¶nderimi â†’ WhatsApp'a yÃ¶nlendir function submitForm(e) { e.preventDefault(); const waNumber = '905451332859'; const form = e.target; const fields = []; form.querySelectorAll('input, select, textarea').forEach(el => { const label = el.placeholder || el.name || ''; const value = (el.value || '').trim(); if (value) fields.push(`${label}: ${value}`); }); const message = 'Merhaba, gaziantepbinatemizlik.com Ã¼zerinden teklif almak istiyorum.\\n\\n' + fields.join('\\n'); const url = `https://wa.me/${waNumber}?text=${encodeURIComponent(message)}`; window.open(url, '_blank'); form.reset(); return false; } // Kayan giriÅŸ animasyonu const observer = new IntersectionObserver((entries) => { entries.forEach(entry => { if (entry.isIntersecting) { entry.target.style.opacity = '1'; entry.target.style.transform = 'translateY(0)'; } }); }, { threshold: 0.1 }); document.querySelectorAll('.service-card, .region-card, .why-card, .testimonial, .feature').forEach(el => { el.style.opacity = '0'; el.style.transform = 'translateY(20px)'; el.style.transition = 'opacity 0.6s ease, transform 0.6s ease'; observer.observe(el); });"